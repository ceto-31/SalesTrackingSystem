# backend/.htaccess
# CORS is handled in PHP (backend/middleware/auth.php) — do NOT set CORS
# headers here, otherwise responses will contain duplicate values.

<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>
